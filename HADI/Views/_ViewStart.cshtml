@{
    if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
    {
        Layout = "_Administration";
    }
    else if (User.Identity.IsAuthenticated && User.IsInRole("Manager"))
    {
        Layout = "_CheckerLayout";
    }
    else
    {
        Layout = "_Layout";
    }
}
